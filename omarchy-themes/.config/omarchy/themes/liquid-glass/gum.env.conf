# Liquid Glass — gum
#
# Shipped rather than generated, for the same reason as btop.theme: what
# Omarchy would generate from a palette is not what these prompts should be.
# There is no theme colour anywhere now — this is white on black.

env = FOREGROUND,##E0E0E0
env = BACKGROUND,##000000
env = BORDER_FOREGROUND,##FFFFFF
env = BORDER_BACKGROUND,##000000
